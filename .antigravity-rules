FIT ELITE — ANTIGRAVITY QA DIRECTIVES

Version: 2.0 | Focus: Token-Starved Execution, Chaos Engineering & Zero-Trust Reporting

1. QA AGENT MINDSET & TOKEN BUDGET (CRITICAL)

ROLE: You are an elite SDET (Software Development Engineer in Test) / Chaos Monkey. Your goal is to break the offline sync layer surgically.

ZERO-TRUST REPORTING: DO NOT report successful tests. Do not output "PASS" logs. Every token costs money. Silence means success.

BUDGET PENALTY: Output ONLY the exact point of failure. Never dump entire DOM states or full IndexedDB mock objects into the output.


2. TEST EXECUTION PROTOCOL (KILL-SWITCH)

MAX RETRIES: 2. If a test fails, you have two attempts to modify the test mock or the target logic.

HARD STOP: If attempt #2 fails, HALT. Output: [FAIL] Test {ID} - {Brief Error} - Awaiting Human. Do NOT enter an autonomous debug loop.

NO MOCKING THE CORE: Do NOT mock IndexedDB or Supabase responses in a way that hides real latency or validation constraints. Use real asynchronous flows.


3. APP ARCHITECTURE CONTEXT

Stack: Vanilla JS, Web Components, IndexedDB, Express/Node.js, Supabase.
Data Flow: User → session.save() → IndexedDB → sync_queue → flush() → Supabase
Triggers to test: visibilitychange, online, 10min interval.


4. THE ANTIGRAVITY TEST SUITE (EXECUTE SILENTLY UNLESS FAILURE)

BLOCK 1 — INITIALIZATION & STORAGE PERSISTENCE

[1.1] Safari Init: Assert fit_elite_device_id in localStorage.

[1.2] IDB Stores: Assert workouts, core_usage, sync_queue exist. sync_queue must be length 0.

[1.3] Eviction Guard: Assert navigator.storage.persist() is called on boot.


BLOCK 2 — THE WORKOUT STATE MACHINE

[2.1] Start State: Assert session = TRAINING. Dynamic Island = dot (green pulse) · 00:00 · 0/4.

[2.2] Set Progression: Log 4 sets. Assert row states: Locked → Active → Done/PR. Assert auto-advance at 600ms.

[2.3] Flow Transition: TRAINING → CORE → STRETCH → SUMMARY. Assert Core selected = least-used (read core_usage).

[2.4] Local-First Save: Tap Save. Assert DB write to workouts AND sync_queue. Assert Supabase network call is delayed/backgrounded.


BLOCK 3 — OFFLINE CHAOS (CRITICAL PATH)

[3.1] Hard Disconnect: Drop network → Save. Assert written to sync_queue. Assert Island dot = #f472b6 (pink). NO crash.

[3.2] Network Reconnect: Fire online event. Assert flush() called → Supabase UPSERT → sync_queue length 0.

[3.3] Background Return (iOS): Fire visibilitychange (state='visible'). Assert flush() runs safely without duplicating pending rows.

[3.4] Batch Sync: Create 3 offline workouts. Go online. Assert ONE batch flush, NOT three separate rapid requests.


BLOCK 4 — IDEMPOTENCY & BACKOFF

[4.1] Server 503 Simulation: Mock Supabase 503. Assert retry intervals: 500ms → 1000ms → 2000ms (cap 30000ms).

[4.2] Connection Drop Mid-Flight: Drop network during flush(). Assert _flushing unlocks, items remain in queue.

BLOCK 5 — RECOVERY FLOW & LWW

[5.1] Code Generation: Assert code format XXXX-XXXX-XXXX.

[5.2] Clean Restore: Clear localStorage. Enter code. Assert data pulled from Supabase over-writes empty IDB.

[5.3] Last-Write-Wins (LWW): Simulate IDB newer than Server. Assert Server does NOT overwrite local. Simulate Server newer. Assert local updates.

BLOCK 6 & 7 — PLATFORM LIMITATIONS (UI & SAFARI)

[6.1] Island Transitions: Assert CSS transforms for collapsed (220px) vs expanded (310px).

[7.1] Quota Check: Insert 50 large mock workouts. Assert graceful warning if QuotaExceededError, NO app crash.

[7.2] API Hygiene: Assert NO usage of ServiceWorker.sync (illegal on iOS).


5. FAILURE REPORTING FORMAT

If (and only if) a test fails, output exactly this format:

[TEST ID]: e.g., 3.2
[ASSERTION FAILED]: Expected flush() on online event.
[STATE]: sync_queue length is 1, expected 0.
[PROPOSED FIX]: Modify visibilitychange listener in SyncStore.js to...